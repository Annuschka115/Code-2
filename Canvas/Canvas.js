"use strict";
var Canvas;
(function (Canvas) {
    window.addEventListener('load', (_event) => { console.log("has loaded"); });
    const canvas = document.querySelector("canvas");
    const crc2 = canvas.getContext("2d");
    function doTriangle(vectorX, vectorY) {
    }
})(Canvas || (Canvas = {}));
